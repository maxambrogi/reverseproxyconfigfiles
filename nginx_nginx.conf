
#user  nobody;
worker_processes  1;

error_log  logs/error.log  info;


#pid        C:\nginx-1.22.0\logs\nginx.pid;


events 
{
    worker_connections  1024;
}


http 
{
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  C:/nginx-1.22.0/logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;

    #server 
	  #{
    #      listen       80;
    #      server_name  localhost;

        #charset koi8-r;

    #     location / 
	  #	    {
    #          root   html;
    #          index  index.html index.htm;
    #       }

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
   #     error_page   500 502 503 504  /50x.html;
   #      location = /50x.html 
	 #	    {
   #         root   html;
   #      }
   # }





    # HTTPS server
    #
    server 
	{
        listen       443 ssl;
        server_name  external.hostname.lab;

        ssl_certificate      C:/nginx-1.22.0/ssl/cert.crt;
        ssl_certificate_key  C:/nginx-1.22.0/ssl/key.key;

        ssl_session_cache    shared:SSL:1m;
        ssl_session_timeout  5m;

        ssl_ciphers  HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers  on;

        location / {
            root   html;
            index  index.html index.htm;
        }
		
	
		
		location /img/ {
			subs_filter internal.hostname.dev external.hostname.lab
			proxy_pass https://internal.hostname.dev/img/;
		}
		

		
		location /dsws/ {
			proxy_pass https://internal.hostname.dev:8443/dsws/;
		}
		

    }
}
